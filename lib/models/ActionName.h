#pragma once

enum ActionName {
    FOLD,

    APPROACH,
    TRANSPORT,
    CATCH,
    
    EXTRACT_STAGE,
    PRERELEASE_STAGE,
    RELEASE_STAGE,
    RELEASE_UP_STAGE,
    RELEASE_GRABBER,

    S1,
    S2,

    BANNER_DEPLOY,

    __ACTION_COUNT
};